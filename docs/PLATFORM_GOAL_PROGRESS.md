# EFDS platform goal progress

Updated: 2026-09-24. This is the working handoff for the ten-milestone Goal. Do not infer completion from code or green tests alone; verify live state.

## Current audit

- `efds-knowledge-base`: `main` at `b33e309` was pushed. The live Alembic head is `dd2fb9d36bb2`; the sender-scoped Outlook tables are empty until delegated consent and a first sync. The auth email hook is version 4 and the provider-event endpoint is version 1. A controlled reset email reached the owner and opened the correct Set your password page. Resend shows Delivered for two other signup recipients who report no inbox arrival; both Auth accounts remained unconfirmed at the last check. Brevo fallback and delivery webhooks remain unverified live. The Slack archive workflow had a successful scheduled run on 23 September at 19:04 UTC, but posting reminders remains blocked by the bot's missing `chat:write` scope.
- `efds-site`: `main` at `8e93b6e` was pushed. It includes scanner-resistant `/auth/confirm`, a dedicated `/signup` page, immediate basic-member access after email confirmation, account review, membership gating, the onboarding DOCX, an admin email-health panel, committee-scoped Slack suggestions, admin proposal review, and an admin-only Outlook ticket-evidence option that remains disabled until an Outlook sync. The site passed lint, 115 unit tests, production build and targeted desktop/tablet/mobile ticket-suggestion browser checks. The `8e93b6e` GitHub website check and Vercel deployment status passed; verify the deployed agent/site together with real accounts.
- `efds-agent`: `main` at `768a087` was pushed. Basic members map to public knowledge, `efds_member` to member knowledge, committee tickets use committee-scoped Slack retrieval, and Outlook ticket evidence is admin-only. The full 39-test suite passed locally with sockets permitted; deployed-service verification remains.
- `efds-recruiting`: active `codex/application-generation` is 31 commits ahead of `origin/main`, with four modified files and two untracked files. Treat these as active work; do not reset or blindly merge. Classify changes and reconcile safely later.
- Outlook scope: the user has limited ingestion to two exact sender addresses in their personal mailbox. These addresses and the personal mailbox identifier are intentionally omitted from this public repository. No Outlook connector was exposed to this session when checked. Do not scan other mail. A suitable delegated connection is required for live ingestion.

## Milestone status and next evidence

| Milestone | State | Evidence / next action |
| --- | --- | --- |
| 1 Auth email fallback | Partial | A controlled owner reset was accepted by Resend, arrived in the inbox, and its new link opened Set your password. Two recipients reported missing signup mail although Resend showed Delivered. At the owner's request, one fresh setup email each was sent through the production site on 24 September at 06:46 UTC; Supabase recorded new confirmation send times and Resend accepted both. Inbox arrival is pending; investigate Junk/quarantine and Imperial Exchange message trace as in `AUTH_EMAIL_FALLBACK.md`. A separate confirmed user previously reported a reset failure; the newest on-screen error has not been captured. Verify signup completion and a controlled Brevo failover before calling the flow reliable. |
| 2 Email reliability | Implemented; provider setup pending | Live migration adds private delivery states and admin-only aggregate health. Version 4 hook records message IDs and makes one Resend idempotent retry. The signed/bearer provider-event function is deployed. The active twice-daily GitHub health/retention job passed a manual first run on 24 September: 6 Resend acceptances, no recorded failures, and 3 older acceptances lacking delivery callbacks. No scheduled email-health run has been observed yet. Register provider webhooks and test live bounce/delivery events; see `AUTH_EMAIL_FALLBACK.md`. |
| 3 Google login | Blocked on provider credentials | A live public Supabase Auth settings check on 2026-09-23 returned `google: false` and `email: true`. The site integration is present and the project-specific guide is `efds-site/docs/GOOGLE_LOGIN_SETUP.md`. Create the Google OAuth web client, enter its client ID/secret in Supabase and enable the provider; then verify live account linking, callbacks and sessions. |
| 4 Membership permissions | Implemented; live E2E pending | Authenticated, confirmed accounts get basic `member` and dashboard access without admin review; only EFDS verification and higher roles need approval. Basic members retain public-level knowledge visibility. A rolled-back live SQL test verified that a basic member cannot obtain verified or committee access. Test with two real confirmed accounts. |
| 5 Account review | Implemented; live E2E pending | Admin-only `review_efds_account` checks actor, version and role transitions, and writes an audit event. Rolled-back live tests verified a grant, audit entry, stale-version rejection, owner-only ordinary edits and a member's own claim submission. The owner's Imperial account had mistakenly remained a basic member; a guarded live maintenance transaction restored it to `admin` with an explicit audit reason on 24 September. The published admin panel is at `/admin/accounts`; verify visibility and a real promotion in the owner's browser. |
| 6 Slack repost | Scheduler running; posting blocked by Slack scope | Fresh `#actions-tickets` sync completed and a scheduled archive-refresh workflow passed on 23 September at 19:04 UTC. The current bot token lacks `chat:write`; a dry run found at least ten eligible historic tickets and the posting command safely skipped. The twice-daily workflow imports new tickets and plans at most five reminders after each successful sync, excludes completed/cancelled dashboard tickets, checks current and archived reaction status and requires a fresh archive. Add the scope and reinstall the Slack app; see `SLACK_TICKET_REPOST.md`. |
| 7 Outlook ingestion | Code and schema ready; consent pending | Live `dd2fb9d36bb2` migration added admin-only sender-scoped mail storage and retrieval. KB sync, agent mode and site option are pushed. A rolled-back live SQL test proved indexing and the member/admin permission boundary. No personal email has been fetched; a delegated Entra app/client ID and user consent are required. See `OUTLOOK_INGESTION.md`. |
| 8 AI draft workflow | Implemented locally; live integrations pending | Committee users can ask the agent for a cited suggestion from committee-visible Slack and review/edit it before publication. Admins can also use meeting, full Slack or connected Outlook evidence in a private operational proposal that requires admin review before committee publication. A rolled-back live SQL test verified proposed → approved → committee publication with linked evidence. Site tests and browser checks passed. Verify the deployed agent/site together with real committee/admin sessions and a populated Outlook source. |
| 9 Onboarding/verification | Partial | The committee Markdown and DOCX handoff describe member → EFDS member → committee → roster link, and `/signup` explains immediate basic membership. Local browser tests cover account review and signup at desktop, tablet and mobile sizes. Real account onboarding remains blocked for recipients who cannot locate their delivered confirmation emails. |
| 10 Repo/OneDrive | Partial | Repo audit above. OneDrive copy and access not yet verified. |

## Active next action

Trace the two missing signup emails through recipients' Junk/quarantine folders and Imperial Exchange; verify the next scheduled email-health run; connect the delegated Outlook app within the two-sender scope; register provider delivery callbacks; add `chat:write` for Slack reposting; configure Google OAuth; complete live committee/admin onboarding and AI proposal checks; reconcile active recruiting and OneDrive work.

## Operating record

- The user authorized publishing validated EFDS work to `main` and `origin`.
- There is no active Goal token budget.
- Do not mark any milestone live verified solely from a configured secret or mocked test.
- One controlled password-reset email was sent to the account owner on 24 September after announcing the test. No Slack reminder messages were posted by this Goal.
