# EFDS platform goal progress

Updated: 2026-09-24. This is the working handoff for the ten-milestone Goal. Do not infer completion from code or green tests alone; verify live state.

## Current audit

- `efds-knowledge-base`: `main` at `5002a07` was pushed before this handoff update. The live Alembic head is `dd2fb9d36bb2`; the sender-scoped Outlook tables are empty until delegated consent and a first sync. The auth email hook is version 4 and the provider-event endpoint is version 1. A controlled reset email reached the owner and opened the correct Set your password page. Resend shows Delivered for two other signup recipients who report no inbox arrival; both Auth accounts remained unconfirmed at the last check. Brevo fallback and delivery webhooks remain unverified live. The Slack archive workflow completed a manual refresh on 24 September at 07:58 UTC. The hourly freshness gate, added after a missed/delayed target schedule, passed a non-forced live check at 08:01 UTC: 16 enabled channels were fresh, so no redundant sync or repost ran. Posting reminders remains blocked by the bot's missing `chat:write` scope; an automatic hourly trigger has not yet been observed.
- `efds-site`: `main` at `6214724` was pushed. It includes scanner-resistant `/auth/confirm`, a dedicated `/signup` page, immediate basic-member access after email confirmation, account review, membership gating, the onboarding DOCX, an admin email-health panel, committee-scoped Slack suggestions, admin proposal review, and an admin-only Outlook ticket-evidence option that remains disabled until an Outlook sync. The password form now distinguishes access-check failure, password-update failure, and a successful update followed by workspace failure; this improves diagnosis but does not establish the cause of the separate user's earlier reset failure. The site passed lint, 118 unit tests, production build and targeted desktop/tablet/mobile ticket-suggestion browser checks. The `6214724` GitHub website check and Vercel deployment status passed; verify the deployed agent/site together with real accounts.
- `efds-agent`: `main` at `768a087` was pushed. Basic members map to public knowledge, `efds_member` to member knowledge, committee tickets use committee-scoped Slack retrieval, and Outlook ticket evidence is admin-only. The full 39-test suite passed locally with sockets permitted; deployed-service verification remains.
- `efds-recruiting`: `main` and `origin/main` now both point to `6e5aa8f`. A separate clean integration checkout selected reusable campaign code, tests and generic documentation from the active `codex/application-generation` branch, excluding its personal execution ledger and generated materials. It passed 313 tests (2 skipped), Ruff, mypy and a wheel build; the wheel contains the new modules and document template. The integration was pushed to main on 24 September. The original application-generation checkout remains clean at `6d09d7d`; it has 38 branch-only commits relative to main, while main has one integration-only commit. Do not reset or merge that private campaign branch wholesale.
- OneDrive recruiting: the mounted copy had been replaced from local source on 22 September, then its `main` ref was reset to the older `origin/main` on 23 September. On 24 September its 267 tracked files were compared to that old commit; only line endings differed. The current clean `efds-recruiting` main source was mirrored into OneDrive. Its `main` and `origin/main` now both point to `6e5aa8f`, and all 333 tracked files match the local clean-main checkout byte for byte. An archive of the previous tracked source and Git metadata is in `efds-recruiting-before-main-20260924` beside the OneDrive project; the earlier 22 September backup also remains. Ignored `.env`, virtual environment, exports, applications and other runtime data were left untouched. Local verification receipt: `/home/siheon/projects/.efds-sync/20260924/sync-result.json`. GVFS returned inconsistent bytes during the first manifest scan; the archived files were independently verified against the old Git commit (242 exact and 25 CRLF-only) and the manifest was reconciled to the archived bytes before proceeding.
- Outlook scope: the user has limited ingestion to two exact sender addresses in their personal mailbox. These addresses and the personal mailbox identifier are intentionally omitted from this public repository. No Outlook connector was exposed to this session when checked. Do not scan other mail. A suitable delegated connection is required for live ingestion.

## Milestone status and next evidence

| Milestone | State | Evidence / next action |
| --- | --- | --- |
| 1 Auth email fallback | Partial | A controlled owner reset was accepted by Resend, arrived in the inbox, and its new link opened Set your password. Two recipients reported missing signup mail although Resend showed Delivered. At the owner's request, one fresh setup email each was sent through the production site on 24 September at 06:46 UTC; Supabase recorded new confirmation send times and Resend accepted both. Inbox arrival is pending; investigate Junk/quarantine and Imperial Exchange message trace as in `AUTH_EMAIL_FALLBACK.md`. A separate reset reporter says password setting failed but does not know the on-screen error. The account is confirmed and eligible but has no recorded sign-in session or site profile, and no password update after its last recovery request on 23 September at 15:15 UTC, before the updated email-template commit. The successful comparison account has a session/profile but no recorded recovery request, so the paths cannot yet be treated as equivalent. The deployed password form now labels the failing step, but a fresh attempt by the affected user is still needed: capture only the page path and visible error if it fails again, never the link or password. Verify signup completion and a controlled Brevo failover before calling the flow reliable. |
| 2 Email reliability | Implemented; provider setup pending | Live migration adds private delivery states and admin-only aggregate health. Version 4 hook records message IDs and makes one Resend idempotent retry. The signed/bearer provider-event function is deployed. The active twice-daily GitHub health/retention job passed a manual first run on 24 September: 6 Resend acceptances, no recorded failures, and 3 older acceptances lacking delivery callbacks. No scheduled email-health run has been observed yet. Register provider webhooks and test live bounce/delivery events; see `AUTH_EMAIL_FALLBACK.md`. |
| 3 Google login | Blocked on provider credentials | A live public Supabase Auth settings check on 2026-09-23 returned `google: false` and `email: true`. The site integration is present and the project-specific guide is `efds-site/docs/GOOGLE_LOGIN_SETUP.md`. Create the Google OAuth web client, enter its client ID/secret in Supabase and enable the provider; then verify live account linking, callbacks and sessions. |
| 4 Membership permissions | Implemented; live E2E pending | Authenticated, confirmed accounts get basic `member` and dashboard access without admin review; only EFDS verification and higher roles need approval. Basic members retain public-level knowledge visibility. A rolled-back live SQL test verified that a basic member cannot obtain verified or committee access. Test with two real confirmed accounts. |
| 5 Account review | Implemented; live E2E pending | Admin-only `review_efds_account` checks actor, version and role transitions, and writes an audit event. Rolled-back live tests verified a grant, audit entry, stale-version rejection, owner-only ordinary edits and a member's own claim submission. The owner's Imperial account had mistakenly remained a basic member; a guarded live maintenance transaction restored it to `admin` with an explicit audit reason on 24 September. The published admin panel is at `/admin/accounts`; verify visibility and a real promotion in the owner's browser. |
| 6 Slack repost | Archive refreshed; posting blocked by Slack scope | The 23 September scheduled run and a 24 September 07:58 UTC manual run passed. The 03:17 UTC target run was not observed by 07:55 UTC, so main `5002a07` added an hourly 41-minute freshness check around the 03:17/15:17 targets. The live non-forced check at 08:01 saw 16 enabled fresh channels and skipped; automatic catch-up remains unobserved and GitHub cron is still best effort. The current bot token lacks `chat:write`; the manual run safely skipped posting. The workflow imports new tickets and plans at most five reminders after each successful sync, excludes completed/cancelled dashboard tickets, checks current and archived reaction status and requires a fresh archive. Add the scope and reinstall the Slack app; see `SLACK_TICKET_REPOST.md` and `SLACK_SCHEDULING.md`. |
| 7 Outlook ingestion | Code and schema ready; consent pending | Live `dd2fb9d36bb2` migration added admin-only sender-scoped mail storage and retrieval. KB sync, agent mode and site option are pushed. A rolled-back live SQL test proved indexing and the member/admin permission boundary. No personal email has been fetched; a delegated Entra app/client ID and user consent are required. See `OUTLOOK_INGESTION.md`. |
| 8 AI draft workflow | Implemented locally; live integrations pending | Committee users can ask the agent for a cited suggestion from committee-visible Slack and review/edit it before publication. Admins can also use meeting, full Slack or connected Outlook evidence in a private operational proposal that requires admin review before committee publication. A rolled-back live SQL test verified proposed → approved → committee publication with linked evidence. Site tests and browser checks passed. Verify the deployed agent/site together with real committee/admin sessions and a populated Outlook source. |
| 9 Onboarding/verification | Partial | The committee Markdown and DOCX handoff describe member → EFDS member → committee → roster link, and `/signup` explains immediate basic membership. Local browser tests cover account review and signup at desktop, tablet and mobile sizes. Real account onboarding remains blocked for recipients who cannot locate their delivered confirmation emails. The password form now identifies the failed step on a future attempt; this has not been tested by the affected user. |
| 10 Repo/OneDrive | Substantially complete | Site `6214724`, agent `768a087`, knowledge-base `5002a07` (before this handoff update) and recruiting `6e5aa8f` are on their pushed main branches. Recruiting's reusable code was integrated with 313 passing tests, Ruff, mypy and a built wheel; its private campaign branch remains separate. The OneDrive recruiting source, Git refs and 333 tracked-file hashes were verified against local main. Runtime files were not changed. Monitor for another external OneDrive Git reset; no ongoing bidirectional sync was configured. |

## Active next action

Trace the two missing signup emails through recipients' Junk/quarantine folders and Imperial Exchange; have the separate password-reset reporter try one fresh reset and capture only the page path and visible error if it fails; verify the next scheduled email-health run; connect the delegated Outlook app within the two-sender scope; register provider delivery callbacks; add `chat:write` for Slack reposting; configure Google OAuth; complete live committee/admin onboarding and AI proposal checks. Keep the private recruiting campaign branch separate from the published reusable tooling and monitor the OneDrive copy for an external reset.

## Operating record

- The user authorized publishing validated EFDS work to `main` and `origin`.
- There is no active Goal token budget.
- Do not mark any milestone live verified solely from a configured secret or mocked test.
- One controlled password-reset email was sent to the account owner on 24 September after announcing the test. One fresh account email was then sent to each of two recipients at the owner's request on 24 September at 06:46 UTC. A manual Slack archive sync was run on 24 September; it did not post reminders because `chat:write` remains missing. No additional email was sent to the separate password-reset reporter.
