# EFDS platform goal progress

Updated: 2026-09-24. This is the working handoff for the ten-milestone Goal. Do not infer completion from code or green tests alone; verify live state.

## Current audit

- `efds-knowledge-base`: `main` at `b1422d9` includes the deployed email-hook update and a bounded, rotating Outlook deletion-reconciliation improvement. The live Alembic head is `0f2a6357bea2`: the account-review RPC no longer requires a free-text reason, while retaining admin-only authorization, optimistic version checks and audit events. A rollback-only live test passed reason-free verification, decline and admin promotion. The sender-scoped Outlook tables are empty until delegated consent and a first sync. The auth email hook is version 5 and the provider-event endpoint is version 1. A controlled reset email reached the owner and opened the correct Set your password page. Resend shows Delivered for two other signup recipients who report no inbox arrival; both Auth accounts remained unconfirmed at the last check. Brevo fallback and delivery webhooks remain unverified live. The Slack archive workflow completed a manual refresh on 24 September at 07:58 UTC. The hourly freshness gate, added after a missed/delayed target schedule, passed a non-forced live check at 08:01 UTC: 16 enabled channels were fresh, so no redundant sync or repost ran. A scheduled GitHub Actions run started at 08:29 UTC, passed the freshness step and correctly skipped sync/import/repost because no refresh was due. Posting reminders remains blocked by the bot's missing `chat:write` scope; an automatic stale-data catch-up has not yet been observed.
- `efds-site`: `main` at `c2459df` was pushed and its Vercel status is successful. It includes scanner-resistant `/auth/confirm`, a dedicated `/signup` page, immediate basic-member access after email confirmation, account review, membership gating, the onboarding DOCX, an admin email-health panel, committee-scoped Slack suggestions, admin proposal review, and an admin-only Outlook ticket-evidence option that remains disabled until an Outlook sync. The site also includes a public Sponsors page and homepage roster with the committee-supplied Optiver and Cornerstone Research founding-partner tiers, Jane Street sponsor tier, and a direct partnership email. For committee/admin users, Society Operations now leads the navigation and `/dashboard` combines ticket work with admin-only knowledge/review health; the former `/admin` overview redirects there. The account review form no longer asks for a reason, and the missing optional officer ID is parsed correctly. The password form now distinguishes access-check failure, password-update failure, and a successful update followed by workspace failure; this improves diagnosis but does not establish the cause of the separate user's earlier reset failure. The integration-health panel now labels missing delivery callbacks and empty email activity accurately and shows the last recorded Outlook sync without claiming the connection is still live. The site passed lint, 128 unit tests, a production build, six focused desktop/tablet/mobile integration-health browser checks and earlier targeted ticket, sponsor and account-review checks. Local preview screenshots are in `efds-site/artifacts/integration-health/`. A read-only production browser smoke check previously confirmed the homepage roster, Sponsors page, email link and legacy Partners redirect at desktop and mobile sizes. Verify the deployed agent/site together with real accounts.
- `efds-agent`: `main` at `768a087` was pushed. Basic members map to public knowledge, `efds_member` to member knowledge, committee tickets use committee-scoped Slack retrieval, and Outlook ticket evidence is admin-only. The full 39-test suite passed locally with sockets permitted; deployed-service verification remains.
- `efds-recruiting`: `main` and `origin/main` now both point to `6e5aa8f`. A separate clean integration checkout selected reusable campaign code, tests and generic documentation from the active `codex/application-generation` branch, excluding its personal execution ledger and generated materials. It passed 313 tests (2 skipped), Ruff, mypy and a wheel build; the wheel contains the new modules and document template. The integration was pushed to main on 24 September. The original application-generation checkout remains clean at `6d09d7d`; it has 38 branch-only commits relative to main, while main has one integration-only commit. Do not reset or merge that private campaign branch wholesale.
- OneDrive recruiting: the mounted copy had been replaced from local source on 22 September, then its `main` ref was reset to the older `origin/main` on 23 September. On 24 September its 267 tracked files were compared to that old commit; only line endings differed. The current clean `efds-recruiting` main source was mirrored into OneDrive. Its `main` and `origin/main` now both point to `6e5aa8f`, and all 333 tracked files match the local clean-main checkout byte for byte. An archive of the previous tracked source and Git metadata is in `efds-recruiting-before-main-20260924` beside the OneDrive project; the earlier 22 September backup also remains. Ignored `.env`, virtual environment, exports, applications and other runtime data were left untouched. Local verification receipt: `/home/siheon/projects/.efds-sync/20260924/sync-result.json`. GVFS returned inconsistent bytes during the first manifest scan; the archived files were independently verified against the old Git commit (242 exact and 25 CRLF-only) and the manifest was reconciled to the archived bytes before proceeding.
- Outlook scope: the user has limited ingestion to two exact sender addresses in their personal mailbox. These addresses and the personal mailbox identifier are intentionally omitted from this public repository. An Outlook Email plugin is available but remains unconnected after earlier suggestions; it could permit a controlled one-time read if connected, but the scheduled ingestion code still needs the delegated Entra app and consent. Do not scan other mail.

## 24 September 09:10 UTC verification checkpoint

- The public Supabase Auth settings, queried using the project's publishable key, still report `email: true` and `google: false`. Google account linking cannot be live-tested until the provider is configured.
- A read-only Slack `auth.test` request succeeded and returned an OAuth scope header that does not include `chat:write`. Keep incomplete-ticket reposting disabled until the app scope is added and the bot is reinstalled.
- GitHub Actions lists one successful *scheduled* Slack refresh at 08:29 UTC. Its logs showed the freshness gate skipping a redundant refresh. The authentication-email health workflow has only its successful manual run at 06:34 UTC; its first post-setup scheduled window is 16:43 UTC, and a scheduled run must be observed before claiming schedule reliability.
- Public DNS lookups found a DMARC TXT record, Resend's `resend._domainkey` TXT record, and SPF TXT and MX records on `send.imperial-efds.com`. These do not prove inbox delivery or Brevo domain authentication; Brevo's own domain dashboard must be checked against the exact records it requested. [Brevo's authentication guide](https://help.brevo.com/hc/en-us/articles/12163873383186-Authenticate-your-domain-with-Brevo-Brevo-code-DKIM-DMARC) explains why its DKIM records may be one TXT or two CNAMEs.

## Milestone status and next evidence

| Milestone | State | Evidence / next action |
| --- | --- | --- |
| 1 Auth email fallback | Partial | A controlled owner reset was accepted by Resend, arrived in the inbox, and its new link opened Set your password. Two recipients reported missing signup mail although Resend showed Delivered. At the owner's request, one fresh setup email each was sent through the production site on 24 September at 06:46 UTC; Supabase recorded new confirmation send times and Resend accepted both. A read-only check at 08:17 UTC on 24 September still found both accounts unconfirmed with no sessions or site profiles; investigate Junk/quarantine and Imperial Exchange message trace as in `AUTH_EMAIL_FALLBACK.md`. A separate reset reporter previously said password setting failed but did not know the error. A fresh read-only check found a session and site profile created at 07:24 UTC on 24 September, followed by an Auth user update at 07:24:51 UTC. This is progress from the previous no-session/no-profile state, but the timestamps alone do not prove that the chosen password works; Supabase Auth audit entries were absent for that period. Ask the reporter to try normal EFDS Login with the password they chose and report only the visible result. If that fails, request one fresh reset and capture the page path and exact on-screen error; never collect the link or password. The deployed password form now labels the failing step. The Auth user has a stored password hash, but only a normal Login attempt can establish whether the chosen password works. The secure email-change form and hook version 5 passed local tests and are live; no controlled two-address confirmation has been performed. Verify signup completion, secure email change and a controlled Brevo failover before calling the flow reliable. |
| 2 Email reliability | Implemented; provider setup pending | Live migration adds private delivery states and admin-only aggregate health. Version 5 hook records message IDs and makes one Resend idempotent retry. The signed/bearer provider-event function is deployed. The active twice-daily GitHub health/retention job passed a manual first run on 24 September: 6 Resend acceptances, no recorded failures, and 3 older acceptances lacking delivery callbacks. No scheduled email-health run has been observed yet. Site `c2459df` makes the admin panel distinguish no recent mail, pending callbacks and recorded failures; Vercel deployed it, while desktop/tablet/mobile behavior was checked locally only. Register provider webhooks and test live bounce/delivery events; see `AUTH_EMAIL_FALLBACK.md`. |
| 3 Google login | Blocked on provider credentials | A live public Supabase Auth settings check on 2026-09-23 returned `google: false` and `email: true`. The site integration is present and the project-specific guide is `efds-site/docs/GOOGLE_LOGIN_SETUP.md`. Create the Google OAuth web client, enter its client ID/secret in Supabase and enable the provider; then verify live account linking, callbacks and sessions. |
| 4 Membership permissions | Implemented; live E2E pending | Authenticated, confirmed accounts get basic `member` and dashboard access without admin review; only EFDS verification and higher roles need approval. Basic members retain public-level knowledge visibility. A rolled-back live SQL test verified that a basic member cannot obtain verified or committee access. Test with two real confirmed accounts. |
| 5 Account review | Implemented; live E2E pending | Admin-only `review_efds_account` checks actor, version and role transitions, and writes an audit event. The live `0f2a6357bea2` migration removed mandatory reasons; a rollback-only test passed reason-free verification, decline and admin promotion with audit events. Site `9550941` removes the reason input and fixes absent officer ID validation, the cause of the reported `invalid_request` for ordinary reviews. The owner's Imperial account had mistakenly remained a basic member; a guarded live maintenance transaction restored it to `admin` with an explicit audit reason on 24 September. The panel is at `/admin/accounts`; verify one real decision in the owner's browser after deployment. |
| 6 Slack repost | Archive refreshed; posting blocked by Slack scope | The 23 September scheduled run and a 24 September 07:58 UTC manual run passed. The 03:17 UTC target run was not observed by 07:55 UTC, so main `5002a07` added an hourly 41-minute freshness check around the 03:17/15:17 targets. The live non-forced check at 08:01 saw 16 enabled fresh channels and skipped. A scheduled run at 08:29 passed the freshness step and skipped downstream work because no refresh was due; an automatic catch-up on stale data remains unverified and GitHub cron is still best effort. The current bot token lacks `chat:write`; the manual run safely skipped posting. The workflow imports new tickets and plans at most five reminders after each successful sync, excludes completed/cancelled dashboard tickets, checks current and archived reaction status and requires a fresh archive. Add the scope and reinstall the Slack app; see `SLACK_TICKET_REPOST.md` and `SLACK_SCHEDULING.md`. |
| 7 Outlook ingestion | Code and schema ready; consent pending | Live `dd2fb9d36bb2` migration added admin-only sender-scoped mail storage and retrieval. KB sync, agent mode and site option are pushed. A rolled-back live SQL test proved indexing and the member/admin permission boundary. The collector now rotates a bounded 200 point rechecks per sender per run instead of refusing to sync once 1,000 older messages exist; tests cover an archive of 1,001 messages and confirmation after a six-hour interval. No personal email has been fetched; a delegated Entra app/client ID and user consent are required. See `OUTLOOK_INGESTION.md`. |
| 8 AI draft workflow | Implemented locally; live integrations pending | Committee users can ask the agent for a cited suggestion from committee-visible Slack and review/edit it before publication. Admins can also use meeting, full Slack or connected Outlook evidence in a private operational proposal that requires admin review before committee publication. A rolled-back live SQL test verified proposed → approved → committee publication with linked evidence. Site tests and browser checks passed. Verify the deployed agent/site together with real committee/admin sessions and a populated Outlook source. |
| 9 Onboarding/verification | Partial | The committee Markdown and DOCX handoff now use the current `/signup` entry and reason-free review flow; the DOCX text, ZIP structure and signup hyperlink were checked locally. Local browser tests cover account review and signup at desktop, tablet and mobile sizes. Real account onboarding remains blocked for recipients who cannot locate their delivered confirmation emails. The password form now identifies the failed step on a future attempt; this has not been tested by the affected user. |
| 10 Repo/OneDrive | Substantially complete | Site `c2459df`, agent `768a087`, knowledge-base `b1422d9` plus this handoff, and recruiting `6e5aa8f` are on their pushed main branches. Recruiting's reusable code was integrated with 313 passing tests, Ruff, mypy and a built wheel; its private campaign branch remains separate. The OneDrive recruiting source, Git refs and 333 tracked-file hashes were verified against local main. Runtime files were not changed. Monitor for another external OneDrive Git reset; no ongoing bidirectional sync was configured. |

## Active next action

Trace the two missing signup emails through recipients' Junk/quarantine folders and Imperial Exchange; ask the separate password-reset reporter whether normal Login now works (a session/profile appeared at 07:24 UTC), using one fresh reset only if it does not; verify the next scheduled email-health run and the first stale-data Slack catch-up; test secure email change with a controlled pair of addresses; connect the delegated Outlook app within the two-sender scope; register provider delivery callbacks; add `chat:write` for Slack reposting; configure Google OAuth; complete live committee/admin onboarding and AI proposal checks. Keep the private recruiting campaign branch separate from the published reusable tooling and monitor the OneDrive copy for an external reset.

## Operating record

- The user authorized publishing validated EFDS work to `main` and `origin`.
- There is no active Goal token budget.
- Do not mark any milestone live verified solely from a configured secret or mocked test.
- One controlled password-reset email was sent to the account owner on 24 September after announcing the test. One fresh account email was then sent to each of two recipients at the owner's request on 24 September at 06:46 UTC. A manual Slack archive sync was run on 24 September; it did not post reminders because `chat:write` remains missing. No additional email was sent to the separate password-reset reporter. No live email change was triggered while deploying the scanner-safe flow.
